#version 330 core
out vec4 FragColor;
in vec4 pixelColor; // the input variable from the vertex shader (same name and same type)
uniform vec4 mycolor;
void main()
{
   FragColor = vec4(1.0f, 0.0f, 0.0f, 1.0f);
   FragColor = mycolor;
}
